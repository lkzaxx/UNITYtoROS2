version: '3.8'

services:
  # 主要的 ROS2 TCP Endpoint 服務
  ros2-tcp-endpoint:
    image: osrf/ros:humble-desktop
    container_name: unity_ros2_tcp
    network_mode: host  # 使用主機網路模式，不需要 ports 映射
    environment:
      - ROS_DOMAIN_ID=0
      - RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
    volumes:
      # 源碼目錄映射
      - ./src:/ros2_ws/src
      - ./scripts:/scripts
    # 注意: 使用 network_mode: host 時，ports 映射會被忽略，所以已移除
    command: ["/bin/bash", "-c", "
        set -e &&
        echo '🔧 更新套件列表...' &&
        apt-get update &&

        echo '📦 安裝 Cyclone DDS...' &&
        apt-get install -y ros-humble-rmw-cyclonedds-cpp &&

        echo '📦 安裝 ROS-TCP-Endpoint...' &&
        apt-get install -y ros-humble-ros-tcp-endpoint ||
        (
          echo '⚠️ ros-tcp-endpoint 不在官方庫中，從源碼安裝...' &&
          apt-get install -y git python3-pip &&
          cd /ros2_ws/src &&
          if [ ! -d ros_tcp_endpoint ]; then
            git clone https://github.com/Unity-Technologies/ROS-TCP-Endpoint.git ros_tcp_endpoint;
          fi
        ) &&

        echo '📦 安裝 Python 依賴...' &&
        apt-get install -y python3-pip &&
        pip3 install numpy transforms3d pyyaml &&

        echo '🔨 建置 ROS2 工作區...' &&
        cd /ros2_ws &&
        source /opt/ros/humble/setup.bash &&
        export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp &&

        if [ -d src/ros_tcp_endpoint ] || [ -d src/unity_openarm_bridge ]; then
          apt-get install -y python3-colcon-common-extensions &&
          colcon build --symlink-install &&
          source install/setup.bash;
        fi &&

        echo '🚀 啟動 TCP Endpoint 伺服器...' &&
        source /opt/ros/humble/setup.bash &&
        export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp &&

        if [ -f install/setup.bash ]; then
          source install/setup.bash;
        fi &&

        ros2 run ros_tcp_endpoint default_server_endpoint --ros-args -p ROS_IP:=0.0.0.0 -p ROS_TCP_PORT:=10000 &

        echo '⏳ 等待 TCP Endpoint 啟動...' &&
        sleep 5 &&

        echo '🔄 啟動 Unity 橋接節點...' &&
        if [ -f /scripts/unity_tcp_bridge.py ]; then
          python3 /scripts/unity_tcp_bridge.py;
        elif [ -f /ros2_ws/install/unity_openarm_bridge/lib/unity_openarm_bridge/tcp_bridge_node.py ]; then
          python3 /ros2_ws/install/unity_openarm_bridge/lib/unity_openarm_bridge/tcp_bridge_node.py;
        else
          echo '⚠️ 找不到橋接節點，保持 TCP Endpoint 運行...' &&
          tail -f /dev/null;
        fi
      "]
    stdin_open: true
    tty: true
    restart: unless-stopped

  # ROS2 工具和監控容器
  ros2-tools:
    image: osrf/ros:humble-desktop
    container_name: ros2_tools
    network_mode: host
    environment:
      - ROS_DOMAIN_ID=0
      - RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
    volumes:
      - ./src:/ros2_ws/src
      - ./scripts:/scripts
    depends_on:
      - ros2-tcp-endpoint
    command: ["/bin/bash", "-c", "
        echo '🔧 設定 ROS2 工具環境...' &&
        apt-get update &&
        apt-get install -y ros-humble-rmw-cyclonedds-cpp &&

        source /opt/ros/humble/setup.bash &&
        export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp &&

        echo '📊 ROS2 工具容器就緒' &&
        echo '' &&
        echo '可用命令範例：' &&
        echo '  ros2 topic list' &&
        echo '  ros2 topic echo /unity/heartbeat' &&
        echo '  ros2 topic echo /openarm/joint_states' &&
        echo '  ros2 service list' &&
        echo '' &&
        exec bash
      "]
    stdin_open: true
    tty: true
